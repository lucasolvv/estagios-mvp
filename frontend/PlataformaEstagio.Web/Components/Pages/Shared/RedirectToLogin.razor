@inject NavigationManager Nav
@code {
    protected override void OnInitialized()
    {
        var here = new Uri(Nav.Uri);
        if (!here.AbsolutePath.Equals("/", StringComparison.OrdinalIgnoreCase))
            Nav.NavigateTo("/?returnUrl=" + Uri.EscapeDataString(Nav.Uri), replace: true);
    }
}
